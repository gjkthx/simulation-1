export default class ShelfB extends Component{
    render(){
        return(
            
    function sortinv(arr){
        for(var i = 0; i < arr.length; i++ ){
            if(arr.bin[i] === "Bin1"){
                this.setState({bin1Empty:false})
            }else if(arr.bin[i] === "Bin2"){
                this.setState({bin1Empty:false})
            }else if(arr.bin[i] === "Bin3"){
                this.setState({bin1Empty:false})
            }else if(arr.bin[i] === "Bin4"){
                this.setState({bin1Empty:false})
            }else if(arr.bin[i] === "Bin5"){
                this.setState({bin1Empty:false})
        }
    }
}
           
        );
    }
}
